'use strict';

describe('Service: gdriveHistoryService', function() {
  // ngInject
  var gdriveHistoryService;

  beforeEach(function() {
    module('chessLoggerApp');
    inject(function(_gdriveHistoryService_) {
      gdriveHistoryService = _gdriveHistoryService_;
    });
  });

  it('should write unit test!', function() {
    this.fail('write me!');
  });
});
